// interfa

import CardTitle from "../common/Card/CardTitle";

const DashboardCardTitle = ({ title }: { title: string }) => {
  return <CardTitle className="mb-4">{title}</CardTitle>;
};

export default DashboardCardTitle;
