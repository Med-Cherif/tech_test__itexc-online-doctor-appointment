import React from "react";

const AppLayoutContent = ({ children }: { children: React.ReactNode }) => {
  return <div className="app-layout-content">{children}</div>;
};

export default AppLayoutContent;
